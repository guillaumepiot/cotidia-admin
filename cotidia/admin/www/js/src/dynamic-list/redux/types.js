import * as bootstrap from './modules/bootstrap/types'
import * as config from './modules/config/types'
import * as modal from './modules/modal/types'
import * as search from './modules/search/types'

export default {
  ...bootstrap,
  ...config,
  ...modal,
  ...search,
}
